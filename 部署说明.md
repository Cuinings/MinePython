# MinePython 部署说明（小白版）

> 看这一篇就够了。每一步都给你可直接复制的命令，不用懂原理也能跑起来。  
> 适合人群：第一次部署、不懂 Docker / 后端也没关系。

---

## 一、这是什么？

MinePython 是一个**本地文件管理 + 审计 + APK 一键安装**的网页平台。

- 它跑在你的电脑（或服务器）上，启动后是一个**网站**。
- 用浏览器打开 `http://localhost:8000` 就能使用：登录、上传/下载文件、管理用户、看审计日志。
- 不需要连外网、不需要买服务器，纯本地就能用。

两种启动方式，任选其一：

| 方式                   | 适合谁             | 难度    | 说明                   |
| -------------------- | --------------- | ----- | -------------------- |
| **方式 A：本地直接跑**       | 想快速试用的小白        | ⭐ 最简单 | 装个 Python，敲几行命令就能用   |
| **方式 B：Docker 一键部署** | 想长期稳定运行 / 像正式上线 | ⭐⭐    | 需要装 Docker，但一次配置长期可用 |

> 推荐新手先试 **方式 A** 跑通，再决定要不要上 Docker。

---

## 二、方式 A：本地直接跑（最简单）

### 第 1 步：安装 Python

1. 打开 <https://www.python.org/downloads/> ，下载并安装 **Python 3.10 或更高版本**。
2. **安装时务必勾选 `Add Python to PATH`**（很关键，否则命令找不到）。
3. 验证安装成功，打开「命令提示符（CMD）」或「终端」输入：

```bash
python --version
```

能看到类似 `Python 3.13.x` 就成功了。

### 第 2 步：进入项目目录

在终端里进入这个项目的文件夹（换成你自己的实际路径）：

```bash
cd C:\Users\xxx\xxx\MinePython
```

### 第 3 步：安装依赖（只需一次）

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

> 看到刷一堆安装信息、最后没报错就对了。这步可能要一两分钟。

### 第 4 步：启动！

```bash
python server.py
```

看到类似 `Application startup complete` 就成功了。

### 第 5 步：打开网站

浏览器访问：**<http://localhost:8000>**

### 默认管理员账号（第一次登录必须改密码）

| 项目  | 值          |
| --- | ---------- |
| 用户名 | `admin`    |
| 密码  | `admin123` |

> ⚠️ 第一次登录会被强制要求改密码，照提示改掉即可。**别留默认密码**，不安全。

---

## 三、方式 B：Docker 一键部署（推荐长期使用）

### 第 1 步：安装 Docker

1. 下载 Docker Desktop：<https://www.docker.com/products/docker-desktop/>
2. 安装时一路下一步，关键：**启用 WSL 2 引擎**（Windows 默认会装好）。
3. 安装完打开 Docker Desktop，等它左下角显示绿点「running」。

验证（终端输入）：

```bash
docker --version
docker compose version
```

> 注意是 `docker compose`（中间空格），不是老的 `docker-compose`。

### 第 2 步：准备密钥文件（不准备会崩溃）

在**项目目录**里执行这一句，生成一个加密密钥文件：

```bash
python -c "import base64,os; open('.fernet_key','wb').write(base64.urlsafe_b64encode(os.urandom(32)))"
```

> 这个文件很重要，丢了会导致已存数据无法解密。它会自动随容器保存，别删、别提交到 git。

### 第 3 步：准备配置文件

复制一份配置模板：

```bash
copy .env.example .env
```

用记事本打开 `.env`，**至少把管理员密码改掉**（默认 `admin123` 千万别留）：

```ini
ADMIN_PASSWORD=换成你自己的强密码
```

其他项可以不动，用默认的就行。

### 第 4 步：一键启动

```bash
docker compose up -d --build
```

第一次会拉取镜像、装依赖，大约 1–3 分钟。

### 第 5 步：验证

```bash
docker compose ps
```

状态显示为 `healthy` 或 `running` 就成功了。浏览器访问：**<http://localhost:8000>**

看日志（出问题排查用）：

```bash
docker compose logs -f
```

---

## 四、第一次登录后必做

1. 打开 `http://localhost:8000` → 用 `admin / admin123`（或你改过的密码）登录。
2. 系统会强制你**改管理员密码**，照做。
3. 之后别人要注册账号，需要你在「用户管理」里**审批通过**才能登录。

---

## 五、常见问题（踩坑速查）

### 1. 启动后访问 `localhost:8000` 打不开

- 确认终端里 `python server.py` 还在运行（别关窗口）。
- 确认端口没被别的程序占用。

### 2. Docker 容器一直重启 / 日志报 `Fernet key` 或 `IsADirectoryError`

说明 `.fernet_key` 没生成好，或生成成了空文件。重新执行「方式 B 第 2 步」生成密钥，然后：

```bash
docker compose down
docker compose up -d --build
```

### 3. 上传文件 / 写数据报 `Permission denied`（Windows）

把项目文件夹整体挪到 WSL2 文件系统里（如 `\\wsl$\Ubuntu\home\你的用户名\MinePython`），再重新 `docker compose up -d --build`。

### 4. 手机 / 其他电脑访问不了

- 手机和电脑要在**同一个 WiFi** 下。
- 查电脑局域网 IP：`ipconfig` 看「IPv4 地址」，手机浏览器访问 `http://电脑IP:8000`。
- 放行防火墙 8000 端口（管理员终端）：

```bash
netsh advfirewall firewall add rule name="FileServer" dir=in action=allow protocol=TCP localport=8000
```

- 如果浏览器报 CORS 拦截，在 `.env` 里把 `CORS_ORIGINS` 加上 `http://电脑IP:8000`，然后重启容器。

### 5. `/docs` 接口文档打不开，显示 403

这是**正常的安全设计**，不是部署失败。生产模式下 API 文档默认关闭。  
本地想看文档，在 `.env` 里加 `APP_DEBUG=true` 再重启即可（仅调试用，正式别开）。

---

## 六、想用手机装 APK（一键安装）？

这是进阶功能，需要额外准备：

1. 电脑装 [Android SDK Platform-Tools](https://developer.android.com/tools/releases/platform-tools)（含 `adb`）。
2. 手机开「USB 调试」（开发者选项）。
3. 浏览器必须用 **Chrome / Edge**，且页面要走 **HTTPS**（因为 WebUSB 要求安全环境）。
4. 生成自签证书：

```bash
python gen_cert.py
```

然后在 `.env` 里设置：

```ini
SSL_CERTFILE=/app/ssl/cert.pem
SSL_KEYFILE=/app/ssl/key.pem
```

重启容器即可用 HTTPS 访问。**手机不要打开这个网页**，在你连着手机的电脑浏览器里操作。

---

## 七、日常运维命令（Docker）

```bash
docker compose ps                 # 看状态
docker compose logs -f            # 看实时日志
docker compose restart            # 改了配置后重启
docker compose down               # 停止（数据不丢）
docker compose up -d --build      # 改了代码后重建
```

上传的文件、数据库、日志都在项目目录里，容器重建也不会丢。

---

## 八、一句话总结

- 想**最快试试**：`python server.py` → 打开 `localhost:8000` → 登录改密码。
- 想**长期稳定运行**：装 Docker → 生成 `.fernet_key` → `docker compose up -d --build`。
- 记住：**改默认管理员密码**、**别删 `.fernet_key`**。

> 更详细的技术文档（配置项全表、API 说明、数据库迁移等）见项目里的 `README.md`。
